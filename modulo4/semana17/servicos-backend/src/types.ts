export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type Address = {
   estado: string,
   cidade: string,
   bairro: string,
   logradouro: string
}